#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	
	int i;
	float x, y;
	
	printf ("Enter x, j, y value: ");
	scanf ("%f%d%f", &x, &i, &y);
	
	printf ("%f,%d,%f", x, i, y);
	
	
	return 0;


/* if input 12.3		45.6	789 , then x = 12.3, i = 45, and y = 0.6*/

}
